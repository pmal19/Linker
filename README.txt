README

1. To compile, run 'javac Linker.java'
2. The input to the program is in the form of a file passed as an argument. Save the input to a text file.
3. Run 'java Linker <Input File Name>' to get the output on the terminal.